extends base.dt

block content
	form#login(method="post", action='/login')
		fieldset 
			legend login
			- if(_error)
				p(style='color:red')= _error
			input.first(type="email", placeholder="email", name="email", required, autofocus)
			input(type="password", placeholder="password", name="password", required)
			input.last(type="submit", value="login")
				