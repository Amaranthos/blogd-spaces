       |import vibe.d;
       |
       |shared static this() {
      1|        auto filesServerSettings = new HTTPFileServerSettings;
      1|        filesServerSettings.serverPathPrefix = "/static/";
      1|        filesServerSettings.options = HTTPFileServerOption.failIfNotFound;
       |
      1|        auto router = new URLRouter;
      1|        router.get("/", staticTemplate!"index.dt");
      1|        router.get("/static/*", serveStaticFiles("public/", filesServerSettings));
       |
      1|        auto settings = new HTTPServerSettings;
      1|        settings.port = 8080;
      1|        settings.bindAddresses = ["::1", "127.0.0.1"];
      1|        listenHTTP(settings, router);
       |
      1|        logInfo("Please open http://127.0.0.1:8080/ in your browser.");
       |}
source\app.d is 100% covered
